# Manta Trace Packet Sniffer Beta Releases

Cross‑platform (macOS/Windows/Linux) desktop app that connects to a USB serial device (your sniffer HW), streams bytes to the UI, and shows them as packets.

